# Slide comments — progress deck

Written by the in-deck review overlay (press `c` on any slide). One block per comment; the HTML comment
carries the anchor (slide id, number, step, pin x/y in 1440×1080 stage px). `- [ ]` open, `- [x]` fixed
(Claude appends `→ fixed in <sha>`). Comments committed from the browser land in `progress/comments/*.md`;
Claude folds them in here when it fixes them.

