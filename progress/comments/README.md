Inbox for slide comments committed from the deck (press `c`, then "commit on GitHub").
One timestamped `.md` file per export. Claude reads these, fixes the slides, and moves each item into
`../COMMENTS.md` with its status.
